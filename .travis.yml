sudo: false
language: python
python:
 - "2.7"
 
install:
  - pip install codecov
  
# command to run tests
script:
 - python -m unittest TestTriangle
 - coverage run TestTriangle.py

after_success:
  - codecov
